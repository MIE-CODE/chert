# API Configuration
# Base URL for the backend API
# Default: http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# WebSocket Configuration
# URL for the WebSocket/Socket.IO server
# If not set, will fall back to NEXT_PUBLIC_API_URL
# Default: http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Note: All environment variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Make sure not to include sensitive information in these variables
